import { Box, Typography } from "@mui/material";

const Complaints = () => {
  return (
    <Box>
      <Typography
        component="h1"
        align="left"
        sx={{ fontWeight: "bold", fontSize: "2rem" }}
      >
        Tenant Complaints
      </Typography>
    </Box>
  );
};

export default Complaints;
